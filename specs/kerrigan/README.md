# Kerrigan legacy specs

`specs/kerrigan-v2/` is the active meta-spec directory and source of truth for how Kerrigan works today.

This directory now keeps only:

- `agents/` — legacy role-spec materials retained for reference
- `_archive-v1/` — archived v1 numbered meta-specs (`000` through `080`)

Use `_archive-v1/` for historical context only.
